# Django Settings
DEBUG=False
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=your-domain.com,localhost

# Database
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Redis & Celery
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# IP Tracking
IP_ANOMALY_THRESHOLD=100
LOG_RETENTION_DAYS=90
SENSITIVE_PATHS=/admin/,/login/,/api/auth/
```

### 2. **Procfile for Deployment** (For Heroku/Render)
**Missing:** `ip_tracking/Procfile`

**Create:** `ip_tracking/Procfile`
```
web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
worker: celery -A config worker --loglevel=info
beat: celery -A config beat --loglevel=info
```

### 3. **Runtime Specification** (Platform Detection)
**Issue:** Your `runtime.txt` is empty

**Fix:** `ip_tracking/runtime.txt`
```
python-3.10.14